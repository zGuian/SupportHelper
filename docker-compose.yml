version: '3.9'

services:
    couchdb:
        image: couchdb:latest
        container_name: couchdb_developed
        environment:
            COUCHDB_USER: administrator@supportHelper
            COUCHDB_PASSWORD: W2knaft@123supportHelper
        ports:
            - "5984:5984"
        volumes:
            - couchdb_data:/opt/couchdb/data
        networks:
            - backend_dev_network

volumes:
    couchdb_data:

networks:
    backend_dev_network:
        driver: bridge